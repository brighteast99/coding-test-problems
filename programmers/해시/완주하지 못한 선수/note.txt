단순히 set으로 변환해서 차집합을 구하면 되나? 싶었으나, 동명이인의 존재로 해당 방법은 불가능.
completion을 순회하면서 participant에서 하나씩 remove하면? n^2으로 시간초과.
그냥 각 배열을 동시에 순회하면서 set에 이름이 없으면 넣고, 있으면 빼는 식으로 쭉 진행하면?
마지막에는 set에 이름 하나만 남지 않을까?
participant가 completion보다 하나 많으니, 하나를 pop해서 먼저 set에 넣어두면 순회도 편할 것으로 보임.